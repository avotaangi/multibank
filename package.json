{
  "name": "multibank",
  "version": "1.0.0",
  "description": "MultiBank - Telegram Web App",
  "scripts": {
    "build": "cd client && npm run build",
    "dev": "cd client && npm run dev",
    "deploy": "vercel --prod"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}
