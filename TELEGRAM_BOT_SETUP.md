# 🤖 Настройка Telegram Bot

## ❌ Текущий статус

**Бот не работает** - отсутствует `TELEGRAM_BOT_TOKEN` в файле `.env`

## 🔧 Как настроить бота

### 1. Получите токен бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/mybots`
3. Выберите вашего бота `@MultibankYo_bot`
4. Перейдите в "API Token"
5. Скопируйте токен

### 2. Настройте .env файл на сервере

```bash
ssh root@178.217.98.35
cd /opt/multibank/telegram-bot
nano .env
```

Обновите следующие значения:

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=ВАШ_РЕАЛЬНЫЙ_ТОКЕН_ЗДЕСЬ
TELEGRAM_BOT_USERNAME=MultibankYo_bot

# API Configuration
API_URL=http://localhost:3001/api

# Web App Configuration
WEBAPP_URL=http://178.217.98.35

# Environment
NODE_ENV=production
```

### 3. Перезапустите бота

```bash
pm2 restart multibank-bot
```

### 4. Проверьте статус

```bash
# Проверка статуса
pm2 status multibank-bot

# Просмотр логов
pm2 logs multibank-bot --lines 20

# Если все хорошо, вы увидите:
# 🤖 MultiBank Telegram Bot started
# 📱 Bot username: MultibankYo_bot
```

## 📋 Текущая конфигурация

- **Сервер:** 178.217.98.35
- **Директория:** `/opt/multibank/telegram-bot`
- **PM2 процесс:** `multibank-bot`
- **Файл конфигурации:** `/opt/multibank/telegram-bot/.env`

## 🔍 Проверка работы бота

После настройки токена:

1. Откройте Telegram
2. Найдите бота `@MultibankYo_bot`
3. Отправьте команду `/start`
4. Бот должен ответить приветственным сообщением

## 🐛 Решение проблем

### Бот не отвечает

```bash
# Проверьте логи
pm2 logs multibank-bot --lines 50

# Проверьте, что токен правильный
ssh root@178.217.98.35 "cd /opt/multibank/telegram-bot && grep TELEGRAM_BOT_TOKEN .env"

# Перезапустите бота
pm2 restart multibank-bot
```

### Ошибка "TELEGRAM_BOT_TOKEN is required"

Это означает, что токен не установлен или файл `.env` не читается:

```bash
# Проверьте наличие файла
ls -la /opt/multibank/telegram-bot/.env

# Проверьте содержимое (не покажет сам токен, но покажет что файл есть)
cat /opt/multibank/telegram-bot/.env | grep -v TOKEN
```

## 📝 Команды бота

После настройки бот будет поддерживать следующие команды:

- `/start` - Начать работу с ботом
- `/balance` - Проверить баланс
- `/transactions` - Последние транзакции
- `/webapp` - Открыть веб-приложение
- `/help` - Помощь

## ✅ После настройки

Когда бот заработает, вы увидите в логах:

```
🤖 MultiBank Telegram Bot started
📱 Bot username: MultibankYo_bot
🌐 WebApp URL: http://178.217.98.35
```

